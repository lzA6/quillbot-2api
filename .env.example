# ====================================================================
# quillbot-2api 配置文件模板
# ====================================================================
#
# 请将此文件重命名为 ".env" 并填入您的凭证。
#

# --- 核心安全配置 (必须设置) ---
# 用于保护您 API 服务的访问密钥。
API_MASTER_KEY=sk-quillbot-2api-default-key-please-change-me

# --- 部署配置 (可选) ---
# Nginx 对外暴露的端口
NGINX_PORT=8088

# --- Quillbot 凭证 (必须设置) ---
# 1. 登录 https://quillbot.com/
# 2. 按 F12 打开开发者工具，切换到“网络”(Network) 面板。
# 3. 进行一次AI图像生成操作。
# 4. 在网络请求列表中，找到对 "generate/image" 的 POST 请求。
# 5. 在“标头”(Headers) -> “请求标头”(Request Headers) 中，找到并复制完整的 "cookie" 值和 "useridtoken" 值。
# 6. 将它们粘贴到下面的 JSON 结构中。
#
# 支持多账号轮询，只需按格式添加 QUILLBOT_AUTH_2, QUILLBOT_AUTH_3, ...
QUILLBOT_AUTH_1='{"cookie": "在此处粘贴您的完整 Cookie 字符串", "token": "在此处粘贴您的 useridtoken"}'
