services:
  nginx:
    image: nginx:latest
    container_name: quillbot-2api-nginx
    restart: always
    ports:
      - "${NGINX_PORT:-8088}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - quillbot-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quillbot-2api-app
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - quillbot-net

networks:
  quillbot-net:
    driver: bridge
